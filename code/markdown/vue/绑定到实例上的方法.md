
* one

`main.js`
```
Vue.prototype.$fun = 'function'
```

* two

```
let obj = {
    install (Vue, options) {
        console.log(Vue) // vue
        console.log(options) // {a: 1}
        Vue.prototype.$fun = options
    }
}

Vue.use(obj, {
    a: 1
})
```
